<script>
  let count = 0;

  function increment() {
    count += 1;
  }

  function decrement() {
    count -= 1;
  }

  function reset() {
    count = 0;
  }
</script>

<div class="app">
  <header class="app-header">
    <h1>🔥 {{.Title}}</h1>
    <p>Built with Svelte + ReboloLang + Bun.js</p>
  </header>
  
  <main class="app-main">
    <div class="counter-card">
      <h2>Counter Example</h2>
      <p class="counter-display">{count}</p>
      <div class="button-group">
        <button on:click={decrement}>-</button>
        <button on:click={reset}>Reset</button>
        <button on:click={increment}>+</button>
      </div>
    </div>
    
    <div class="info-card">
      <h3>Next Steps:</h3>
      <ul>
        <li>Edit <code>frontend/src/App.svelte</code> to build your UI</li>
        <li>Add components in <code>frontend/src/components/</code></li>
        <li>Run <code>bun run dev</code> to start the dev server</li>
        <li>API endpoints available at <code>/api/*</code></li>
      </ul>
    </div>
  </main>
</div>

<style>
  /* Component-specific styles can go here */
</style>
