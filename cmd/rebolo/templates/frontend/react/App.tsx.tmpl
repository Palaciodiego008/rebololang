import React, { useState } from 'react';

function App() {
  const [count, setCount] = useState(0);

  return (
    <div className="app">
      <header className="app-header">
        <h1>🔥 {{.Title}}</h1>
        <p>Built with React + ReboloLang + Bun.js</p>
      </header>
      
      <main className="app-main">
        <div className="counter-card">
          <h2>Counter Example</h2>
          <p className="counter-display">{count}</p>
          <div className="button-group">
            <button onClick={() => setCount(count - 1)}>-</button>
            <button onClick={() => setCount(0)}>Reset</button>
            <button onClick={() => setCount(count + 1)}>+</button>
          </div>
        </div>
        
        <div className="info-card">
          <h3>Next Steps:</h3>
          <ul>
            <li>Edit <code>frontend/src/App.tsx</code> to build your UI</li>
            <li>Add components in <code>frontend/src/components/</code></li>
            <li>Run <code>bun run dev</code> to start the dev server</li>
            <li>API endpoints available at <code>/api/*</code></li>
          </ul>
        </div>
      </main>
    </div>
  );
}

export default App;
