CREATE TABLE {{.TableName}} (
    id BIGSERIAL PRIMARY KEY,
{{range .Fields}}    {{.DBName}} {{.SQLType}},
{{end}}    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
